# 🏛️ KIS AUTO-TRADING BOT: TRADING CONSTITUTION (절대 변경 금지 헌법)

이 문서는 본 자동매매 봇의 **"타협할 수 없는 절대적 핵심 원칙(Immutable Invariants)"**을 정의합니다.  
어떠한 업데이트, 리팩토링, 기능 추가 시에도 아래의 헌법적 원칙은 **절대로 변경하거나 혼동해서는 안 됩니다.**

---

## 제1조 (포지션 운용 원칙: 3종목 집중 투자)
1. **최대 보유 종목 수 (`MAX_POSITIONS`)는 항상 정확히 3종목으로 고정한다.**
   * 어설픈 5~10종목 백화점식 분산 투자를 엄격히 금지한다.
   * 시장에서 가장 강력한 최상위 3대 주도주에만 자본을 집중 배팅하는 공격형 고수익 모델(+2,048% CAGR, 35.88% 연평균 수익률 실전 백테스트 검증)을 고수한다.
2. **종목당 배정 비중 (`MAX_POSITION_PCT`)은 계좌 자산의 33.3% (약 0.333)를 원칙으로 한다.**
3. `.env` 파일과 `config.py`의 `MAX_POSITIONS` 값은 영구히 `3`으로 일치해야 한다.

---

## 제2조 (타임프레임 원칙: 2~10일 순수 스윙 매매)
1. 본 봇의 전략 타임프레임은 **"2일~10일 단기 파동(5%~15% 수익)을 먹고 빠지는 순수 스윙(Pure Swing)"**이다.
2. **장기·후행성 지표 전면 금지**:
   * **12개월 애널리스트 목표주가**: 장기 밸류에이션 모델로, 단기 폭등 주도주를 상투라고 오판하여 감점하는 모멘텀 킬러이므로 진입 필터로 사용하지 않는다.
   * **45일 지연 의회 매매(STOCK Act)**: 공시 시차가 45일에 달하는 후행성 껍데기 데이터이므로 실시간 스윙 진입 신호로 사용하지 않는다.
3. **승인된 9대 순수 스윙 무기**:
   * 마크 미너비니 VCP 피봇 돌파 (Setup K)
   * 토비 크라벨 NR7 극소 변동성 수축 (`NR7_COIL`)
   * 윌리엄 오닐 RS 상대강도선 20일 선행 신고가 (`RS_LINE_NEW_HIGH`)
   * 칼만 필터(Kalman Filter) 무지연 가속도 기울기
   * 20일 스윙 저점 앵커드 VWAP (AVWAP) 지지선
   * 와이코프 볼륨 고갈 (VDU: 20MA 대비 65% 이하 매물 소진)
   * TTM 변동성 스퀴즈 폭발 (Setup I)
   * 오프닝 레인지 브레이크아웃(ORB Expansion) & 갭페이드 트랩 방어
   * 나스닥 하락 대비 지수 압력 디커플링 (Setup J)

---

## 제3조 (리스크 관리 및 출구 원칙)
1. **자본 회수**: 3일간 박스권 횡보 시 조기 매도 (`DEAD_MONEY_EXIT`)로 자금 회전율 극대화.
2. **분할 익절**: 1.5R 도달 시 50% 분할 익절, 나머지는 본절 스탑 상향 후 추세 추종.
3. **손절 원칙**: 매수가 대비 원칙적 하드 스탑(-4%~-6%) 및 샹들리에 ATR 트레일링 스탑 적용.
4. **꼬리 위험 방어**: CBOE SKEW 지수 140 이상 시 꼬리위험 리스크 감쇄(-4pt 및 손절 타이트).

---

## 제4조 (자동 배포 및 3대 저장소 동기화 절대 원칙)
로컬에서 코드를 수정하거나 최적화했을 경우, **반드시 아래 4단계를 빠짐없이 일괄 실행**한다:
1. `kis-auto-trading` 수정본을 `kis-auto-trading-packaged`로 복사.
2. `kis-auto-trading-packaged`에서 `git add`, `git commit`, `git push origin main` 실행.
3. 오라클 클라우드 VPS(`141.148.172.12`)로 SFTP 업로드 후 `sudo systemctl restart kis-trading.service` 재시작 및 실시간 로그 검증.
4. 배포용 최종 압축 파일 `AI_Swing_Bot_v2026_Final.zip` 재압축 갱신.

---

## 제5조 (무인 자율 운용 보장 원칙)
1. 텔레그램 메시지는 4,096자 초과 시 3,900자 단위로 자동 청크 분할 전송한다.
2. 잔고 부족으로 계산된 수량이 0주이거나 호가 조회 실패 시 증권사 주문 전송을 사전에 차단한다.
3. 점수 산정 시 결측치(NaN/Inf)가 발생해도 프로그램이 크래시되지 않고 0점으로 자동 방어한다.

---

## 제6조 (실전 데이터베이스 및 영속성 보존 절대 원칙)
1. **서버의 `trades.db`, `token.json` 등 실거래 영속 데이터는 절대 로컬 파일로 덮어쓰거나 초기화할 수 없다.**
   * 로컬의 `trades.db`는 템플릿 파일일 뿐이므로, 서버로 SFTP 업로드하는 대상에서 영구히 제외한다.
   * 실거래 체결 이력의 단일 진실 공급원(Single Source of Truth)은 오직 오라클 VPS의 `trades.db`이며, 로컬은 서버로부터 백업(Pull)만 받을 수 있다.
2. **배포 스크립트의 불변 안전장치 (Blacklist Guard)**:
   * 모든 원격 동기화 스크립트는 `trades.db`, `*.db`, `token.json`, `shadow_state.json`을 원격 업로드 금지 블랙리스트에 강제 등록하여 서버의 실전 데이터를 영구히 보호한다.
