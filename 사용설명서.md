# 🤖 AI 스윙 트레이딩 봇 사용설명서 (User Manual)

본 프로그램은 한국투자증권(KIS) Open API를 기반으로 미국의 우량 대형주(Russell 1000)를 대상으로 스윙 매매를 실행하는 지능형 자동 매매 봇입니다. 

초보자도 명령어 하나 없이 더블클릭과 화면 입력을 통해 본인만의 자동 매매 시스템을 로컬 PC 혹은 클라우드 서버에 구축할 수 있도록 설계되었습니다.

---

## 1. ⚙️ 사전 준비 사항

프로그램 구동을 위해 반드시 아래의 **두 가지 핵심 정보**가 필요합니다.

### 🔑 1.1. 한국투자증권 Open API 발급
1. **한국투자증권 홈페이지** 또는 [한국투자증권 개발자센터](https://apiportal.koreainvestment.com/)에 접속하여 로그인합니다.
2. `[계약/키관리]` -> `[Open API KEY 신청]` 메뉴로 이동합니다.
3. 서비스 이용약관 동의 및 본인 인증 후 **APP KEY**와 **SECRET KEY**를 발급받습니다.
   * *주의: 발급 시 반드시 본인의 해외주식 거래가 가능한 종합계좌번호(CANO)를 연동하십시오.*
4. 모의투자로 테스트하고 싶으신 경우, 별도로 `[모의투자 신청]` 메뉴를 거쳐 모의투자용 APP KEY와 SECRET KEY를 추가 발급받아야 합니다.

### 📢 1.2. 텔레그램 실시간 알림 봇 연동
매매 시작, 종료, 체결 내역 및 수익 그래프를 실시간으로 받아보려면 본인의 텔레그램 아이디와 연동해야 합니다.

1. **텔레그램 알림 봇 생성**:
   - 텔레그램 앱에서 **`@BotFather`**를 검색하여 대화방에 입장합니다.
   - `/newbot` 명령어를 입력하고 화면 안내에 따라 봇 이름과 사용자명을 입력합니다.
   - 생성 성공 메시지와 함께 제공되는 **HTTP API Token** (봇 토큰)을 따로 메모해 둡니다. (예: `7183945...:AAH_...`)
2. **사용자 고유 채팅 ID (Chat ID) 확인**:
   - 텔레그램 앱에서 **`@userinfobot`**을 검색하여 대화방에 들어가 메시지를 하나 보냅니다.
   - 봇이 알려주는 **숫자로 된 고유 ID**를 따로 적어 둡니다. (예: `584930219`)
3. **봇 활성화**:
   - 새로 만든 본인의 봇 검색창에 이름(사용자명)을 치고 들어가 `[시작]` 버튼을 클릭해 둡니다. (먼저 말을 걸어두어야 알림 수신이 가능합니다.)

---

## 2. 🚀 프로그램 구동 및 설정 방법

본 프로그램은 컴퓨터 초보자도 안심하고 사용할 수 있도록 **황금빛 새 아이콘 모양의 단독 실행 파일(`AI_Swing_Bot.exe`)**과 **원클릭 구동기(`start.bat`)**를 동시에 제공합니다.

---

### 💻 2.1. 윈도우 PC(로컬)에서 직접 구동하는 경우
개인용 컴퓨터를 켜두고 본인의 윈도우 환경에서 직접 자동매매 봇을 실행할 때의 방법입니다.

1. **`start.bat` (원클릭 구동기) 더블클릭**: 
   - 프로그램이 실행을 시작하며 사용자의 PC에 **파이썬(Python)이 제대로 설치되어 있는지 자동으로 스캔**합니다.
   - **파이썬이 없는 경우**: 즉시 인터넷 브라우저로 파이썬 공식 사이트가 열립니다. 다운로드 받아 설치해 주십시오. (※ 설치 창 맨 아래의 **'Add Python to PATH'** 체크박스를 꼭 체크하셔야 합니다.)
   - **파이썬이 있는 경우**: 봇 작동에 필요한 필수 전문 라이브러리들을 1초 만에 알아서 완전 자동 동기화 설치합니다.
2. **설정 마법사 실행**: 
   - 의존성 검사가 완료되면 고급스러운 다크 테마의 **설정 마법사 GUI 창**이 자동으로 열립니다. (직접 폴더 안의 **`AI_Swing_Bot.exe`**를 더블클릭해서 실행하셔도 됩니다.)
3. **정보 입력 및 연결 검증**:
   - `[KIS 증권 계좌]` 탭에 발급받은 **APP KEY**, **SECRET KEY**, **계좌번호**를 적습니다.
   - `[텔레그램 알림]` 탭에 **봇 토큰**과 **채팅 ID**를 적습니다.
   - 하단의 **`[🔌 연결 검증 및 테스트]`** 버튼을 클릭해 실제 통신에 연동 성공하는지 검증합니다. (연동 성공 시 본인의 텔레그램으로 축하 메시지가 즉시 발송됩니다.)
4. **로컬 구동 확인**:
   - `[구동 및 배포 방식]` 탭에서 **`개인 PC (Windows) 로컬 구동`**을 체크합니다.
   - 하단 오른쪽의 **`[🚀 설정 저장 및 구동 시작]`** 버튼을 누르면 설정 파일(`.env`)이 보안 규격에 맞게 자동 생성되며 즉시 매매 봇이 실행됩니다.
5. **이후 편리한 가동**:
   - 최초 1회 정보 입력이 끝나면, 이후부터는 **`start.bat`**를 더블클릭하여 **`[1] AI 자동 매매 시작`**만 선택하시면 바로 미국주식 스윙 매매가 가동됩니다! (프로그램이 실행되는 검은색 명령창을 열어두시면 매일 밤 알아서 미국 장 거래를 처리합니다.)

---

### ☁️ 2.2. 원격 클라우드 서버(오라클 클라우드 등)에 자동 배포하는 경우
로컬 PC를 매일 밤마다 켜둘 수 없을 때, 가상 서버(Linux)에 봇을 원스톱으로 설치하여 완전히 무인으로 24시간 가동되게 연동해 주는 고급 배포 기능입니다.

1. **사전 준비**: 오라클 클라우드(Oracle Cloud)나 AWS 등에서 생성한 가상 서버의 **서버 IP 주소**와 접속을 위한 **SSH 개인키 파일(id_rsa 또는 .pem)**을 준비합니다.
2. **설정 마법사 켜기**: 폴더 안의 **`AI_Swing_Bot.exe`**를 더블클릭하여 설정 마법사 창을 가동합니다.
3. **개인 정보 입력**: KIS 증권사 계좌 키와 텔레그램 알림용 토큰, 채팅 ID를 각각의 탭에 입력합니다.
4. **클라우드 배포 모드 선택**:
   - `[구동 및 배포 방식]` 탭에서 **`원격 클라우드 서버 (Linux) 자동 배포`**를 선택합니다.
   - 하단 SSH 입력란에 **서버 IP 주소**, **SSH 계정명 (보통 ubuntu)**을 입력하고, 찾아보기 버튼을 눌러 본인의 **SSH 개인키 파일**을 로드합니다.
5. **원스톱 원격 설치 시작**: 
   - 하단 오른쪽의 **`[🚀 설정 저장 및 구동 시작]`**을 클릭한 뒤 최종 확인 창에서 [예]를 누릅니다.
   - 프로그램이 원격 서버에 SSH로 초고속 자동 접속하여, **원격 폴더 자동 생성 ➡️ 봇 소스코드 업로드(SFTP) ➡️ 서버 내 파이썬 라이브러리 자동 설치 ➡️ systemd 백그라운드 구동 서비스 등록 및 즉시 시작**까지 모든 단계를 단 10초 만에 완수합니다!
6. **24시간 가동 완료**:
   - 등록 및 실행 완료 안내가 표시되면 배포가 완전히 끝납니다. 
   - *이후에는 로컬 PC를 언제든지 마음 편히 꺼두셔도, 지구 반대편 클라우드 서버가 매일 밤 미국 장이 개장할 때 알아서 봇을 가동하고 텔레그램으로 모든 거래 보고서를 전송해 줍니다.*

---

## 🛡️ 3. 탑재된 안전 및 리스크 장치들

본 봇 프로그램은 개인 투자 자산의 보호와 하방 리스크 차단을 위해 다음 안전장치를 기본 활성화하고 있습니다:

1. **소액 계좌 안전 30% 룰 (Small Account Safety Filter)**:
   - 총 투자금 대비 1주당 주가가 비정상적으로 비싼 종목(예: $777 계좌 기준 $233.10 초과 주식)은 매수 시도조차 하지 않고 스킵합니다.
   - 단 한 종목에 자금이 40% 이상 집중되어 급락 시 큰 타격을 입는 현상을 구조적으로 차단합니다.
2. **CHOPPY(횡보장) 진입 원천 차단**:
   - 시장 레짐(국면)이 횡보세이거나 변동성이 불안정할 때는 신규 진입(BUY)을 멈추고 현금을 보호합니다.
3. **일일 손실 차단기 (Circuit Breaker)**:
   - 계좌 내 하루 평가 손실이 당일 시작 자산 대비 -3%에 달하는 즉시 회로를 차단하고 신규 주문을 정지합니다.

---

## ❓ 4. 자주 묻는 질문 및 문제 해결 (Q&A)

* **Q: start.bat 실행 시 검은 창이 잠깐 떴다가 사라집니다.**
  * A: 파이썬(Python) 환경 변수 PATH 등록이 제대로 안 되었을 수 있습니다. 파이썬을 다시 재설치하시면서 첫 설치 화면 하단에 있는 **"Add Python to PATH"** 체크박스를 반드시 체크하고 진행해 주세요.
* **Q: 텔레그램 메시지가 오지 않습니다.**
  * A: 텔레그램에서 본인이 새로 만든 알림 봇의 대화방에 들어가 **`[시작]`** 또는 **`/start`**를 꼭 먼저 전송해 주셔야 알림 전송 권한이 발생합니다.
* **Q: 모의투자 계좌로 실행하고 싶은데 자꾸 에러가 납니다.**
  * A: 한국투자증권 모의투자는 실전 계좌 키와 다릅니다. 홈페이지 모의투자 메뉴에서 별도로 신청하여 발급받은 모의투자용 Key를 입력하고, 설정 마법사에서 `[모의투자 계좌]` 체크박스에 체크해 주셔야 정상 구동됩니다.
* **Q: 24시간 장을 계속 감시하며 켜둬야 하나요?**
  * A: 봇은 매일 밤 미국 장(한국 시간 밤 10시 30분 ~ 새벽 5시)에 맞춰 동작하므로, 로컬 실행 시에는 해당 시간대에 컴퓨터를 켜두셔야 하며, 원격 클라우드 배포 모드를 쓰시면 컴퓨터를 언제든 끄셔도 완전히 자유롭게 백그라운드 무중단 구동됩니다.

---

## 🔄 5. 지능형 자동 업데이트 시스템 (Auto-Update)

본 봇 프로그램에는 사용자님이 배포한 최신 전략 알고리즘을 지인들이 스스로 동기화받을 수 있는 **'원클릭 자동 업데이트 엔진(`updater.py`)'**이 탑재되어 있습니다. 지인들은 터미널이나 파일 복사 없이 그저 프로그램을 평소처럼 켜기만 하면 100% 무인 업데이트가 진행됩니다.

### 5.1. 동작 방식
1. **로컬 구동 사용자 (`start.bat` 실행 시)**:
   - `start.bat`을 더블클릭할 때마다 원격 서버(GitHub)의 최신 버전 정보(`version.json`)와 비교합니다.
   - 새 업데이트가 발견되면 최신 전략 코드 파일들(`strategy.py`, `orchestrator.py` 등)을 온라인에서 **자동으로 백그라운드 다운로드하여 안전하게 덮어쓰기 교체**한 뒤 프로그램을 기동합니다.
2. **원격 클라우드 서버 사용자 (실서버 구동 시)**:
   - 매일 밤 미국 장이 개장할 때 백그라운드 매매 프로그램(`main.py`)이 가동을 시작하면서 자동으로 인터넷 업데이트를 검색합니다.
   - 새 패치가 감지되면 원격 서버가 스스로 소스코드를 덮어쓰기 다운로드하고 **자체 프로세스 재기동(Self-Reload)**을 실행하여 즉시 최신 알고리즘으로 미국 주식 매매를 재개합니다.

### 5.2. 개발자(사용자님) 업데이트 배포 설정 방법
사용자님이 로직을 개선하고 친구들에게 업데이트를 릴리즈하고 싶으실 때는 다음 단계만 거치시면 됩니다.

1. 본인의 **공식 깃허브(GitHub) 저장소**를 하나 개설합니다.
2. 수정이 완수된 전략 파일들(`strategy.py` 등)과 `version.json`을 깃허브 저장소에 업로드(Push)합니다.
   - ※ 이때 `version.json` 파일 안의 `"version": "1.0.1"`과 같이 버전 넘버를 높여 주셔야 업데이트로 인식됩니다.
3. 배포 패키지 내의 `updater.py` 14번째 라인에 있는 `RAW_BASE_URL` 변수의 경로를 **본인의 깃허브 raw 파일 주소**로 단 한 번만 변경해 줍니다.
   - 예시: `RAW_BASE_URL = "https://raw.githubusercontent.com/[본인깃허브ID]/[저장소이름]/main/"`
4. **결과**: 이제 친구들은 새로운 파일을 직접 다운받고 교체하는 번거로움 없이, **평소대로 더블클릭만 하면 최신 전략으로 1초 만에 자동 무인 패치되어 안전하게 매매가 돌아갑니다!**
